{
  "name": "consulting-pptx-skill",
  "private": true,
  "description": "HTML デッキの実レンダリング検査（scripts/check_layout.mjs）に使う playwright だけを依存に持つ",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "check:layout": "node scripts/check_layout.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
